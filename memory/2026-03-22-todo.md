# 2026-03-22 — Action Items (When Home)

## 🔧 Infrastructure Fixes
1. **Gmail OAuth re-auth** — `gog auth login` in browser. Token expired/revoked again.
2. **OCI instance** — 163.192.146.82 not responding. Check OCI console (cloud.oracle.com → Compute → Instances). Might be stopped/reclaimed. SSH key: `~/Downloads/ssh-key-2026-03-14.key`
3. **Voice tools missing in session** — TTS and message tools filtered out of tool policy. Debug why. Config has ElevenLabs set up correctly. Might need gateway restart or config change.

## 📱 Reed — Google Play Rejection #3
- **Same reason again:** "Testers were not engaged"
- **Plan:** Post on José's other Reddit account to recruit more testers
- **Also:** Try engaging with existing posts/comments on the feed (check Brave browser, openclaw profile for saved Reddit login)
- **Draft:** Write an appeal/communication for Google Play Console explaining tester engagement with data (6 testers, 56+ feedback items, specific usage patterns)

## 📱 Reed — Share/Import Reader Inconsistency
- **The bug:** When sharing content INTO Reed (e.g., from browser via share intent), the reading experience behaves differently than when opening a book from the library
- **Specific symptom:** Auto-play/pause behavior differs. In library reader: tap screen → shows pause icon (soft pause). In share reader: this doesn't happen correctly — user has to tap pause icon explicitly
- **Root cause (suspected):** TWO separate implementations still exist:
  - `ReaderScreen.kt` + `ReaderViewModel.kt` (library flow)
  - `ClipboardReaderScreen.kt` + `ClipboardReaderViewModel.kt` (clipboard/share flow)
  - `BaseReaderViewModel.kt` (shared base — but clearly not sharing EVERYTHING)
- **The fix should be:** Ensure ClipboardReaderScreen uses the exact same playback control logic as ReaderScreen, or better yet, unify into a single reader screen that handles all content sources
- **File formats supported:** EPUB, MOBI, DXT, Markdown, PDF, plain text — each may have different parsing but should all feed into the SAME reader experience

## 🎙️ Voice-Call Plugin Setup
- **Plugin:** `@openclaw/voice-call` — already bundled, status: disabled
- **Decision:** WhatsApp-first, Pipecat for orchestration, OCI Querétaro for infra
- **But also:** OpenClaw voice-call plugin exists and could give us phone calls faster
- **To do:** Enable plugin, pick provider (Twilio or Telnyx), get MX phone number
- **ElevenLabs config already correct:** voice ID KNHreRJxQ5HN8GisliR9, multilingual v2

## 📋 Full List Summary
1. Gmail OAuth
2. OCI instance
3. Voice tools in session
4. Reed: Reddit recruitment
5. Reed: Google Play appeal draft
6. Reed: Fix share/import reader parity
7. Voice-call plugin setup
